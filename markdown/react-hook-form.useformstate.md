<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [react-hook-form](./react-hook-form.md) &gt; [useFormState](./react-hook-form.useformstate.md)

## useFormState() function

This custom hook allows you to subscribe to each form state, and isolate the re-render at the custom hook level. It has its scope in terms of form state subscription, so it would not affect other useFormState and useForm. Using this hook can reduce the re-render impact on large and complex form application.

**Signature:**

```typescript
declare function useFormState<TFieldValues extends FieldValues = FieldValues>(props?: UseFormStateProps<TFieldValues>): UseFormStateReturn<TFieldValues>;
```

## Parameters

|  Parameter | Type | Description |
|  --- | --- | --- |
|  props | [UseFormStateProps](./react-hook-form.useformstateprops.md)<!-- -->&lt;TFieldValues&gt; | _(Optional)_ include options on specify fields to subscribe. [UseFormStateReturn](./react-hook-form.useformstatereturn.md) |

**Returns:**

[UseFormStateReturn](./react-hook-form.useformstatereturn.md)<!-- -->&lt;TFieldValues&gt;

## Remarks

\[API\](https://react-hook-form.com/docs/useformstate) • \[Demo\](https://codesandbox.io/s/useformstate-75xly)

## Example


```tsx
function App() {
  const { register, handleSubmit, control } = useForm({
    defaultValues: {
    firstName: "firstName"
  }});
  const { dirtyFields } = useFormState({
    control
  });
  const onSubmit = (data) => console.log(data);

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <input {...register("firstName")} placeholder="First Name" />
      {dirtyFields.firstName && <p>Field is dirty.</p>}
      <input type="submit" />
    </form>
  );
}
```

